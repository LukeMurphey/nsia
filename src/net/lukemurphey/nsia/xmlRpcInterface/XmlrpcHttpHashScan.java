package net.lukemurphey.nsia.xmlRpcInterface;

import java.util.*;

public class XmlrpcHttpHashScan {
	
	public boolean createRule( Hashtable<String, Object> rule, int siteGroupId ){
		
		// 0 -- Precondition check
		
		//	0.1 -- Make sure the rule is not null
		if( rule == null )
			return false;
		
		//	0.2 -- Make sure the rule is the correct type
		//rule.get()
		//if( )
		return true;
	}
}
