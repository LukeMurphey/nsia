package net.lukeMurphey.nsia.htmlInterface;

//import net.lukeMurphey.nsia.GeneralizedException;
//import net.lukeMurphey.nsia.NoSessionException;
//import net.lukeMurphey.nsia.trustBoundary.ApiAccessControl;

public class HtmlFirewallManagement extends HtmlContentProvider {

	/*public static ContentDescriptor getHtml(WebConsoleConnectionDescriptor requestDescriptor ) throws GeneralizedException, NoSessionException{
		return getHtml( requestDescriptor, null );
	}
	
	private static ActionDescriptor performAction(WebConsoleConnectionDescriptor requestDescriptor, ApiAccessControl accessControl) throws GeneralizedException, NoSessionException{
		
		// 0 -- Get parameters
		String action = requestDescriptor.request.getParameter("Action");
		
		// 1 -- Adding entry
		if( action != null && action.equals("New")){
			String ruleAction = requestDescriptor.request.getParameter("RuleAction");
			String ruleAddressStart = requestDescriptor.request.getParameter("AddressStart");
			String ruleAddressEnd = requestDescriptor.request.getParameter("AddressEnd");
			
			
		}
		
		// 2 -- Deleting entry
		
		// 3 -- Editing entry
		
		// 4 -- Viewing entries
		 
		
		return new ActionDescriptor( ActionDescriptor.OP_VIEW);
	}*/

}
